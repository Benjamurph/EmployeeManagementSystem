@model IEnumerable<EmployeeManagementSystem.Models.Employee>

@{
    ViewData["Title"] = "Leave Balances";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Employees Leave Balances</h3>
    </div>
    <div class="card-body">
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.EmployeeNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.FullName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AllocatedLeave)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.LeaveBalance)
                    </th>
                    <th>
                        View
                    </th>
                    <th>
                        Action
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.EmployeeNumber)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.FullName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AllocatedLeave)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LeaveBalance)
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning"><i class="fa fa-eye"></i> View</a>
                        </td>
                        <td>
                            <a asp-action="AdjustLeaveBalance" asp-route-id="@item.Id" class="btn btn-primary" style="background-color: #3d66a9; border: #3d66a9"><i class="fa fa-edit"></i> Adjust</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>